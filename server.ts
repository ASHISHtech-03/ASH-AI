import express from "express";
import path from "path";
import { createServer as createViteServer } from "vite";

async function startServer() {
  const app = express();
  const PORT = 3000;

  app.use(express.json());

  // API routes
  app.get("/api/health", (req, res) => {
    res.json({ status: "ok" });
  });

  // Feedback API (Real integration simulation)
  app.post("/api/feedback", (req, res) => {
    const { rating, comment, email, userName } = req.body;
    console.log(`[FEEDBACK] Received from ${userName} (${email}): Rating ${rating}, Comment: ${comment}`);
    // In a real app with SMTP, you'd send an email to the administrator here
    res.json({ 
      success: true, 
      message: "Feedback recorded. Thank you!"
    });
  });

  // Vite middleware for development
  if (process.env.NODE_ENV !== "production") {
    const vite = await createViteServer({
      server: { middlewareMode: true },
      appType: "spa",
    });
    app.use(vite.middlewares);
  } else {
    const distPath = path.join(process.cwd(), 'dist');
    app.use(express.static(distPath));
    app.get('*', (req, res) => {
      res.sendFile(path.join(distPath, 'index.html'));
    });
  }

  app.listen(PORT, "0.0.0.0", () => {
    console.log(`Server running on http://localhost:${PORT}`);
  });
}

startServer();
